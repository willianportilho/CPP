#include "PmergeMe.hpp"

template<typename T>
void	PmergeMe::sort(T &container, size_t begin_index, size_t end_index)
{
	(void)container;
	(void)begin_index;
	(void)end_index;
	std::cout << "teste" << std::endl;
	return ;
}
